# Imposter Game — Project Memory

> Architecture, stack, API, env vars, and game logic are documented in CLAUDE.md — no need to repeat here.

## Learned Patterns & Preferences
- User handles all git commits manually — never commit or push.
- venv is named `impostervenv` (not `venv`).
